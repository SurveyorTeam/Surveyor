<p id="notice"><%= notice %></p>
<h1><%=@current_question.text%><h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% bool_1 = 0 %>
  <% bool_2 = 0 %>
    <% @current_survey_responses.each do |response| %>
      <tr>
        <% if @current_question.kind == 0 %>
          <td><%=  "#{response.text}"  %></td>
        <% end %>
<!--           <% if @current_question.kind == 1 %>
          <% end %> when can submit survey responses to bools -->
		<td><%= link_to 'Destroy', response, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', survey_path(@current_survey) %>
