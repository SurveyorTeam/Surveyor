<p id="notice"><%= notice %></p>

<h1>Listing Surveys</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @surveys.each do |survey| %>
      <tr>
        <td><%= link_to "#{survey.name}", "surveys/#{survey.id}/subjects_survey" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "set demographic", new_demographic_path %>